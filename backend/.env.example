GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3001/auth/google/callback

# Name of the parent Drive folder that all project folders are created inside.
# It's auto-created on first use if it doesn't already exist. Defaults to "UFL Docs".
GOOGLE_DRIVE_ROOT_FOLDER_NAME=UFL Docs

# Optional: skip the name lookup/creation above and use this exact folder ID as the parent instead.
GOOGLE_DRIVE_ROOT_FOLDER_ID=
